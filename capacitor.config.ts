import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.egzdeveloper.lospeinesdero',
  appName: 'Los Peines de Ro',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
